# 👥 Online Status System - AOW Job Platform

## 🎯 ฟีเจอร์ที่สร้าง

### ✅ **Backend System**

**1. OnlineStatus Model** (`backend/models/OnlineStatus.js`)
- เก็บสถานะออนไลน์ของผู้ใช้แต่ละคน
- ข้อมูลเซสชัน: sessionId, sessionStart, sessionDuration
- ข้อมูลอุปกรณ์: browser, OS, mobile/desktop
- ข้อมูลกิจกรรม: lastActivity, currentPage, activityCount
- TTL index: ลบข้อมูลเก่าหลัง 7 วัน

**2. Online Status API** (`backend/routes/onlineStatusRoutes.js`)
- `POST /api/online/heartbeat` - อัปเดตสถานะออนไลน์ (ทุก 30 วินาที)
- `POST /api/online/offline` - ตั้งสถานะออฟไลน์
- `GET /api/online/status` - ดูสถานะตัวเอง
- `GET /api/online/admin/dashboard` - สถิติสำหรับแอดมิน
- `GET /api/online/admin/users` - รายชื่อผู้ใช้ออนไลน์แบบละเอียด
- `POST /api/online/admin/cleanup` - ทำความสะอาดข้อมูล

**3. Audit Logging Integration**
- บันทึกการเข้าถึงข้อมูลออนไลน์สำหรับแอดมิน
- ติดตาม admin actions

### ✅ **Frontend System**

**1. useOnlineStatus Hook** (`src/hooks/useOnlineStatus.js`)
- ส่ง heartbeat ทุก 30 วินาที
- ตรวจจับการปิดหน้าต่าง (beforeunload)
- ตรวจจับการซ่อน/แสดงหน้าต่าง (visibilitychange)
- ตรวจจับ focus/blur events
- ใช้ sendBeacon สำหรับการส่งข้อมูลก่อนปิดหน้า

**2. OnlineStatusWidget Component** (`src/components/OnlineStatusWidget.jsx`)
- แสดงจำนวนผู้ใช้ออนไลน์แยกตาม role
- รายชื่อผู้ใช้ออนไลน์พร้อมรูปโปรไฟล์
- ข้อมูลอุปกรณ์และเบราว์เซอร์
- เวลาออนไลน์และจำนวนกิจกรรม
- สถิติการใช้งาน

**3. Integration ใน App.js และ AdminView**
- เริ่ม online tracking อัตโนมัติเมื่อ login
- แสดง OnlineStatusWidget ในหน้า AdminView
- Handle logout ด้วยการตั้งสถานะออฟไลน์

---

## 🔧 วิธีการทำงาน

### **1. User Login**
```javascript
// เมื่อผู้ใช้ login สำเร็จ
useOnlineStatus(); // เริ่ม tracking
```

### **2. Heartbeat System**
```javascript
// ส่งทุก 30 วินาที
POST /api/online/heartbeat
{
  "sessionId": "session_abc123_1234567890",
  "currentPage": "/jobs"
}
```

### **3. Activity Tracking**
- **Page Changes**: ติดตามหน้าที่ผู้ใช้เยี่ยมชม
- **Session Duration**: คำนวณเวลาออนไลน์
- **Activity Count**: นับจำนวนการกระทำ
- **Device Info**: ตรวจจับ browser, OS, mobile/desktop

### **4. Offline Detection**
- **Manual**: เมื่อ logout
- **Auto**: ไม่มี heartbeat เกิน 5 นาที
- **Browser Close**: ใช้ beforeunload + sendBeacon

### **5. Admin Dashboard**
- **Real-time Stats**: จำนวนผู้ใช้ออนไลน์แยกตาม role
- **User List**: รายชื่อพร้อมข้อมูลละเอียด
- **Activity Stats**: สถิติการใช้งาน

---

## 📊 ข้อมูลที่เก็บ

### **User Session Data:**
```javascript
{
  userId: ObjectId,
  userEmail: "user@example.com",
  userName: "John Doe",
  userRole: "jobseeker",
  isOnline: true,
  sessionId: "session_abc123_1234567890",
  lastActivity: "2024-01-06T10:30:00Z",
  currentPage: "/jobs",
  sessionStart: "2024-01-06T09:00:00Z",
  activityCount: 25,
  deviceInfo: {
    browser: "Chrome",
    os: "Windows",
    device: "Desktop",
    isMobile: false
  },
  ipAddress: "192.168.1.100",
  totalOnlineTime: 5400000 // milliseconds
}
```

### **Admin Dashboard Response:**
```javascript
{
  summary: {
    totalOnline: 15,
    byRole: {
      jobseeker: 8,
      employer: 6,
      admin: 1
    }
  },
  onlineUsers: [...],
  statistics: {
    totalSessions: 15,
    averageSessionTime: 1800000,
    mostActiveUsers: [...]
  }
}
```

---

## 🎨 UI Features

### **Admin Dashboard Widget:**
- 📊 **Summary Cards**: จำนวนผู้ใช้แยกตาม role
- 👥 **User List**: รายชื่อพร้อมรูปโปรไฟล์
- 🕒 **Session Info**: เวลาออนไลน์และกิจกรรม
- 📱 **Device Info**: browser, OS, mobile/desktop
- 📈 **Statistics**: สถิติการใช้งาน
- 🔄 **Auto Refresh**: อัปเดตทุก 30 วินาที

### **User Status Indicators:**
- 🟢 **Online**: จุดสีเขียวกระพริบ
- 👁️ **Current Page**: หน้าที่กำลังดู
- ⏱️ **Session Time**: เวลาออนไลน์
- 📱 **Device**: ไอคอนอุปกรณ์
- 🎯 **Activity**: จำนวนกิจกรรม

---

## 🔒 Security & Privacy

### **Data Protection:**
- ✅ IP Address ซ่อนบางส่วนใน UI
- ✅ Admin-only access สำหรับข้อมูลละเอียด
- ✅ Audit logging สำหรับการเข้าถึงข้อมูล
- ✅ TTL index ลบข้อมูลเก่าอัตโนมัติ

### **Performance:**
- ✅ Efficient MongoDB queries
- ✅ Indexed fields สำหรับ performance
- ✅ Auto cleanup offline users
- ✅ Pagination สำหรับรายชื่อผู้ใช้

---

## 🚀 การใช้งาน

### **สำหรับ Admin:**
1. เข้าหน้า AdminView
2. ดู "ผู้ใช้ออนไลน์" widget
3. คลิก "ดูรายละเอียด" เพื่อดูข้อมูลเพิ่มเติม
4. ดูสถิติการใช้งานและผู้ใช้ที่มีกิจกรรมมาก

### **สำหรับ User:**
- ระบบทำงานอัตโนมัติเมื่อ login
- ไม่ต้องทำอะไรเพิ่มเติม
- สถานะจะอัปเดตอัตโนมัติ

---

## 🧪 การทดสอบ

### **ทดสอบ Backend:**
```bash
# ทดสอบ heartbeat
curl -X POST http://localhost:5000/api/online/heartbeat \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"currentPage": "/test"}'

# ทดสอบ admin dashboard
curl -X GET http://localhost:5000/api/online/admin/dashboard \
  -H "Authorization: Bearer ADMIN_TOKEN"
```

### **ทดสอบ Frontend:**
1. Login ด้วย admin account
2. เปิดหน้า AdminView
3. ดู OnlineStatusWidget
4. เปิดหลายแท็บ/เบราว์เซอร์
5. ตรวจสอบการอัปเดตแบบ real-time

---

## 📈 ข้อมูลที่ Admin จะเห็น

### **Dashboard Overview:**
- **ผู้ใช้ออนไลน์ทั้งหมด**: 15 คน
- **ผู้หางาน**: 8 คน
- **นายจ้าง**: 6 คน  
- **แอดมิน**: 1 คน

### **รายละเอียดผู้ใช้:**
- **ชื่อ**: John Doe
- **อีเมล**: john@example.com
- **บทบาท**: ผู้หางาน
- **หน้าปัจจุบัน**: /jobs
- **เวลาออนไลน์**: 1ช 30น
- **อุปกรณ์**: Chrome on Windows
- **กิจกรรม**: 25 ครั้ง

### **สถิติ:**
- **เซสชันทั้งหมด**: 15
- **เวลาออนไลน์เฉลี่ย**: 30 นาที
- **ผู้ใช้ที่มีกิจกรรมมาก**: Top 5

---

## 🔧 การปรับแต่ง

### **Heartbeat Interval:**
```javascript
// เปลี่ยนจาก 30 วินาที เป็น 60 วินาที
const interval = setInterval(sendHeartbeat, 60000);
```

### **Offline Timeout:**
```javascript
// เปลี่ยนจาก 5 นาที เป็น 10 นาที
lastActivity: { $gte: new Date(Date.now() - 10 * 60 * 1000) }
```

### **Data Retention:**
```javascript
// เปลี่ยนจาก 7 วัน เป็น 30 วัน
{ expireAfterSeconds: 30 * 24 * 60 * 60 }
```

---

**สรุป**: ระบบ Online Status ทำงานแบบ real-time และให้ข้อมูลครบถ้วนสำหรับแอดมินในการติดตามผู้ใช้ออนไลน์ พร้อมรักษาความเป็นส่วนตัวและประสิทธิภาพ