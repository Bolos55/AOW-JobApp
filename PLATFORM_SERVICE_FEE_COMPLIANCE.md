# Platform Service Fee Compliance Guide
## การปรับโครงสร้างให้สอดคล้องกับข้อกำหนดธนาคารและ ธปท.

### 📋 สรุปการเปลี่ยนแปลง

ระบบได้รับการปรับปรุงให้เป็น **Platform Service Fee** เท่านั้น ไม่ใช่ payment gateway, escrow หรือ bill payment service ตามข้อกำหนดของธนาคารและ ธปท.

---

## ✅ การปรับปรุงหลัก

### 1. **Data Model Changes**

#### Payment Model → Service Fee Model
```javascript
// เก่า: Payment-focused
amount: Number,
totalPrice: Number,
pricing: { ... }

// ใหม่: Service Fee-focused  
serviceFee: Number,           // ค่าบริการแพลตฟอร์ม
feeBreakdown: { ... },        // รายละเอียดค่าบริการ
servicePackage: { ... },      // แพ็กเกจบริการ
additionalServices: [{ ... }], // บริการเสริม
serviceTerms: String          // เงื่อนไขการให้บริการ
```

#### Payment ID Format
```javascript
// เก่า: PAY_xxxxx (Payment)
// ใหม่: SVC_xxxxx (Service Fee)
```

### 2. **Terminology Changes**

| เก่า | ใหม่ |
|-----|-----|
| Payment | Service Fee Payment |
| Price/ราคา | Service Fee/ค่าบริการ |
| Package Price | Service Package Fee |
| Boost Price | Additional Service Fee |
| Payment Gateway | Service Fee Collection |
| Payment Verification | Service Fee Verification |

### 3. **API Endpoints**

#### Routes ที่ปรับปรุง
- `POST /api/payments/create` → สร้างการชำระค่าบริการแพลตฟอร์ม
- `GET /api/payments/:id/status` → ตรวจสอบสถานะการชำระค่าบริการ
- `POST /api/payments/:id/cancel` → ยกเลิกการชำระค่าบริการ

#### Audit Log Changes
```javascript
// เก่า: PAYMENT_CREATE
// ใหม่: SERVICE_FEE_CREATE
```

### 4. **Frontend Components**

#### PaymentModal → ServiceFeeModal
- เปลี่ยนชื่อ component และ props
- ปรับ wording ทั้งหมดเป็น "ค่าบริการแพลตฟอร์ม"
- เน้นว่าเป็นการชำระค่าบริการ ไม่ใช่การชำระเงินแทนผู้อื่น

#### Key Changes
```jsx
// เก่า
<PaymentModal onPaymentSuccess={...} />
calculateTotal()
packages.price

// ใหม่  
<ServiceFeeModal onServiceFeeSuccess={...} />
calculateTotalServiceFee()
servicePackages.serviceFee
```

---

## 🏛️ ข้อกำหนดที่ปฏิบัติตาม

### 1. **Platform Service Fee เท่านั้น**
- ✅ เก็บเฉพาะค่าบริการของระบบ (job posting / job boost)
- ✅ ไม่รับเงินแทนผู้ใช้รายอื่น
- ✅ ไม่โอนเงินต่อให้ร้านค้า/บุคคลที่สาม
- ✅ เงินเข้าบัญชี platform โดยตรง

### 2. **ไม่ใช่ Escrow/Payout/Bill Payment**
- ✅ ไม่มีการเก็บเงินไว้แทนผู้อื่น
- ✅ ไม่มีการจ่ายเงินต่อให้บุคคลที่สาม
- ✅ ไม่มีระบบ wallet หรือ balance
- ✅ ไม่มีการรับชำระบิลแทนผู้อื่น

### 3. **Wording และ Data Model**
- ✅ ระบุชัดว่าเป็น "Platform Service Fee"
- ✅ ใช้คำว่า "ค่าบริการแพลตฟอร์ม" แทน "ราคา"
- ✅ เน้นว่าเป็นค่าบริการ ไม่ใช่การซื้อขาย
- ✅ มีเงื่อนไขการให้บริการที่ชัดเจน

---

## 📁 ไฟล์ที่ปรับปรุง

### Backend Files
```
backend/models/Payment.js           → ปรับ schema เป็น service fee
backend/utils/pricingUtils.js       → เปลี่ยนเป็น service fee calculation
backend/utils/paymentUtils.js       → เปลี่ยนเป็น service fee verification
backend/routes/paymentRoutes.js     → ปรับ API endpoints
```

### Frontend Files
```
src/components/ServiceFeeModal.jsx  → ใหม่ (แทน PaymentModal)
src/components/PaymentModal.jsx     → ต้องปรับหรือแทนที่
```

### Documentation
```
PLATFORM_SERVICE_FEE_COMPLIANCE.md → เอกสารนี้
```

---

## 🔄 Migration Plan

### Phase 1: Backend Updates ✅
- [x] ปรับ Payment Model เป็น Service Fee structure
- [x] อัปเดต pricingUtils และ paymentUtils
- [x] ปรับ API routes และ terminology

### Phase 2: Frontend Updates 🔄
- [x] สร้าง ServiceFeeModal component ใหม่
- [ ] แทนที่ PaymentModal ในไฟล์ที่ใช้งาน
- [ ] ปรับ wording ใน UI components อื่นๆ

### Phase 3: Testing & Deployment
- [ ] ทดสอบระบบ service fee ทั้งหมด
- [ ] ตรวจสอบ compliance กับข้อกำหนด
- [ ] Deploy และ monitor

---

## 🚨 สิ่งสำคัญที่ต้องระวัง

### 1. **Backward Compatibility**
- เก็บ legacy fields ไว้เพื่อ compatibility
- ใช้ virtual fields สำหรับ old API calls
- Migration helper functions

### 2. **Legal Compliance**
- เงื่อนไขการให้บริการต้องชัดเจน
- ระบุว่าเป็น platform service fee เท่านั้น
- ไม่รับประกันผลลัพธ์ของการจ้างงาน

### 3. **User Communication**
- แจ้งผู้ใช้เกี่ยวกับการเปลี่ยนแปลง terminology
- อธิบายว่าเป็นค่าบริการแพลตฟอร์ม
- ให้ข้อมูลเงื่อนไขการให้บริการ

---

## 📞 การใช้งาน

### สำหรับ Developer
```javascript
// เก่า
import PaymentModal from './components/PaymentModal';
<PaymentModal onPaymentSuccess={handleSuccess} />

// ใหม่
import ServiceFeeModal from './components/ServiceFeeModal';
<ServiceFeeModal onServiceFeeSuccess={handleSuccess} />
```

### สำหรับ API
```javascript
// เก่า
const payment = await Payment.findById(id);
console.log(payment.amount);

// ใหม่ (ยังใช้ได้เพราะมี virtual field)
const payment = await Payment.findById(id);
console.log(payment.serviceFee); // ใหม่
console.log(payment.amount);     // เก่า (virtual field)
```

---

## ✅ Compliance Checklist

- [x] ระบบเก็บเฉพาะค่าบริการแพลตฟอร์ม
- [x] ไม่มีการรับเงินแทนผู้อื่น
- [x] ไม่มีการโอนเงินต่อให้บุคคลที่สาม
- [x] เงินเข้าบัญชีแพลตฟอร์มโดยตรง
- [x] Wording เป็น "Platform Service Fee"
- [x] Data model สะท้อนการเป็น service fee
- [x] ไม่ใช่ escrow/payout/bill payment
- [x] มีเงื่อนไขการให้บริการที่ชัดเจน

---

**สรุป:** ระบบได้รับการปรับปรุงให้สอดคล้องกับข้อกำหนดธนาคารและ ธปท. โดยเน้นว่าเป็น Platform Service Fee เท่านั้น ไม่ใช่ payment gateway หรือ escrow service